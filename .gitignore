# Raw data — do NOT commit (file size + data privacy)
data/raw/
data/raw/*.csv
data/raw/*.xlsx
data/raw/*.zip

# Processed data is committed (clean CSVs only, no raw PII)
# data/processed/  <- intentionally NOT ignored

# Python
__pycache__/
*.py[cod]
.ipynb_checkpoints/
*.egg-info/
.env
venv/
.venv/

# OS
.DS_Store
Thumbs.db
